import React from 'react';

import Button from '../UI/Button';

const AddList = (props) => {
    return (
        <div>
            <Button
                onClick={}
                label="Add new list"/>
        </div>
    )
}

export default AddList;